# DP Fault AI — GPU machine requirements
# torch is NOT listed here — installed separately in Setup_gpu.bat
# with CUDA 11.8 index URL for GTX 1080 Ti
#
# Versions are intentionally NOT pinned for numpy, frozenlist, matplotlib
# and other packages with compiled wheels — pip will pick the correct
# build for your Python version automatically.
#
# Core ML stack versions ARE pinned — these must match exactly.

# ── core ML stack — pinned ──────────────────
transformers==4.57.6
trl==0.15.2
peft==0.13.2
accelerate==1.13.0

# ── data / tokenization — pinned ────────────
datasets==4.8.4
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub==0.36.2

# ── scientific — unpinned, any recent version works ─
numpy
scipy
matplotlib
pandas
pyarrow

# ── utilities — unpinned ────────────────────
requests
tqdm
packaging
PyYAML
regex
psutil
xxhash
multiprocess
dill
filelock
fsspec
